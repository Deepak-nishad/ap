const inputString = "JavaScript is a scripting language.";
const containsScript = inputString.includes("Script");

// Display the result
console.log(`Does the string contain the word "Script"? ${containsScript}`);
